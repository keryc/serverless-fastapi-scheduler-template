name: Deploy
on:
  workflow_dispatch:
    inputs:
      stage:
        description: 'Serverless stage to deploy (e.g., dev, prod)'
        default: 'prod'
        required: true
      region:
        description: 'AWS region'
        default: 'us-east-1'
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Set up Python 3.11
        run: uv python install 3.11
      - name: Install Serverless & Python deps
        run: |
          npm ci
          uv sync --frozen --no-dev
      # Same export the deploy-dev/deploy-prod Makefile targets run, so the
      # Lambda layer is built from the lockfile instead of a stale committed file.
      - name: Export requirements.txt from uv.lock
        run: uv export --no-dev --no-emit-project --frozen > requirements.txt
      - name: Deploy
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ github.event.inputs.region }}
        run: npx serverless deploy --stage ${{ github.event.inputs.stage }} --conceal
