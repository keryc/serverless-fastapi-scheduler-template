# Stage
STAGE=dev

# AWS
AWS_REGION=us-east-1

# API paths (optional)
API_BASE_PATH=
ROOT_PATH=

# Bearer token for the protected endpoints. Empty => those endpoints return 503.
API_BEARER_TOKEN=

# Schedules are toggled per stage in serverless.yml (custom.stages.*.schedulesEnabled)
